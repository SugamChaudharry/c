#include <stdio.h>
int main() {
    int k=1;
    while (k)
    {
        printf("jai sugam");
        k++;
    }return 0;

    // datatype  specifies the size and type of information will store in varible
    // int - 2-4 byets stores non decimal numbers, char - stores single characters  
}